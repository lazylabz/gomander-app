// Language names are intentionally NOT translated — each language must always
// display in its own language regardless of the active locale (self-referential).
export const LANGUAGE_LABELS: Record<string, string> = {
  en: 'English',
  es: 'Español',
};
